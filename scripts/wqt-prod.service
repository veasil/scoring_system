[Unit]
Description=WQT Auth Backend (Production)
After=network.target

[Service]
User=root
WorkingDirectory=/home/admin/app/scoring_system
ExecStart=/usr/bin/npm run start
Restart=always
Environment=NODE_ENV=production

[Install]
WantedBy=multi-user.target
